{
  "name": "three-mesh-editor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
  "dev": "concurrently \"vite --host 0.0.0.0 --port 5173\" \"python -m uvicorn app:app --host 127.0.0.1 --port 8000 --reload\"",
  "build": "vite build",
  "preview": "vite preview --host 0.0.0.0 --port 5173"
},
  "dependencies": {
    "file-saver": "^2.0.5",
    "three": "^0.167.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^5.4.0"
  }
}
